# Secure USB Factory docs

These docs describe the current `stick`, `vault`, `wipe`, and `forge` CLI model.

Runtime docs copied into generated packages live in `docs/runtime/`.


Start with `25 - public CLI contract.md` when checking the active public command families.
